function ExtractISISpikes(root, tetrode, window)
    % EXTRACTISISPIKES(root, tetrode)
    % Extracts all spikes in the ntt file with an interspike interval less
    % than the given time window.
    % 
    % root: (char[]) the path of the folder containing the NTT file
    % tetrode: (int) number of the tetrode to be analyzed


end
